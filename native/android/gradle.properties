org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# The Expo build next door needs this and this one does not, but they share a
# machine and a developer. Stated explicitly so nobody has to remember which
# project they are in.
android.nonTransitiveRClass=true
