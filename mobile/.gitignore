node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Build logs from scripts/build-ios.sh. Regenerated on every run, and the full
# one is large; share buildlog.txt by pasting it, don't commit it.
buildlog.txt
buildlog.full.txt

# Native projects are generated by `expo prebuild` — regenerate, don't commit.
/android
/ios

# Signing keystores — NEVER commit
/credentials/
*.jks
*.keystore
