# Control-plane build + secrets
node_modules/
dist/
.env
.env.local
*.log

# Runtime data volumes (if ever bind-mounted locally)
data/
pgdata/
caddy_data/
mosquitto/passwordfile
mosquitto/certs/

# Platform Option D1 — do not commit secrets / large models
.env.production
certs/*.key
certs/*private*
face/models/*.onnx
face/Dockerfile.bak.*
