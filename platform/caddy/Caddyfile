# Circuvent edge — Caddy reverse proxy + automatic TLS for the app API.
{
	email admin@circuvent.com
}

# Control-plane REST API + live WebSocket (/ws). Caddy proxies the WS upgrade.
api.circuvent.com {
	encode zstd gzip
	reverse_proxy api:8080
}
